# Generated data artifacts (regenerate by running the pipeline)
raw_data_architecture/data/*
!raw_data_architecture/data/.gitkeep
data_cleaning/data/*
!data_cleaning/data/.gitkeep
dashboard/output/*
!dashboard/output/.gitkeep

# Instructor intellectual property — LOCAL ONLY, never commit (see README IP notice)
# The TiC paper, the market-based deck, and the TiC->TTC conversion workbook,
# plus any lookup tables derived from them, stay out of version control.
local/
docs/*.pdf
*.xlsx
outputs/*
!outputs/.gitkeep

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# Stray legacy output dir (branch uses dashboard/output/)
/output/
