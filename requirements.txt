# Runtime dependencies of the creditrating package (and its CLI).
# Fresh-clone story:  pip install -r requirements.txt -r requirements-dev.txt
# Exact reproducible pins: add  -c constraints.txt
yfinance>=0.2.40
pandas>=2,<4
numpy>=1.21
scipy>=1.7
openpyxl>=3.1
requests>=2.28
pyarrow>=15
PyYAML>=6.0
pydantic>=2.7,<3
structlog>=24
typer>=0.12
