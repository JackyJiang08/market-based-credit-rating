# services/api

Phase 11: a thin service boundary over `creditrating` (rate/batch endpoints,
pydantic domain models as the request/response contract, manifest per run).
Not started; the package API it will wrap is `creditrating.data.pipeline.run`.
